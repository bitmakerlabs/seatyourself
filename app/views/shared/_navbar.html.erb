<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-0">
    <a class="navbar-brand" href="#">Res.io</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <%= link_to "Home", reservations_path, :class => "nav-link"  %>
            </li>
            <li class="nav-item">
              <%= link_to "New Reservation", new_reservation_path, :class => "nav-link"  %>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown">
                  <%= current_user.email %>
                </a>
                <div class="dropdown-menu">
                    <%= link_to "Logout", session_path, method: :delete, :class => "dropdown-item text-center" %>
                </div>
            </li>

        </ul>
    </div>
</nav>
