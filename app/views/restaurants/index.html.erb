<h1> Welcome to opentable v.2</h1>

<section>
  <% @restaurants.each do |restaurant| %>
    <p class="name"><%=link_to(restaurant.name, restaurant_path(restaurant.id))%></p>
    <p>Address: <%=restaurant.address%></p>
    <p>Phone: <%=restaurant.phone_number%></p>
    <p>Total Capacity: <%=restaurant.capacity%></p>
  <% end %>
</section>
