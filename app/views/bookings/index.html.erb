<h1><%= @user.email %>'s reservations</h1>

<h2>You currently have <%= pluralize @bookings.count, "booking" %>!</h2>

<ul>
  <% @bookings.each do |booking| %>
    <li>A table for <%= booking.n_people %> seats at <%= booking.restaurant.name %> for <%= booking.time_to_am_pm %>  on <%= booking.day.strftime("%B %d, %Y") %></li>
  <% end %>
</ul>
