<% if @restaurant.errors.any? %>

    <ul>
    <% @restaurant.errors.full_messages.each do |msg| %>
      <span class="error">*<%= msg %></span>
    <% end %>
    </ul>
<% end %>

<%=form_for @restaurant do |f| %>

<p>
  <%= f.label :main_picture %><br>
  <%= f.text_field :main_picture %>
</p>
<p>
  <%= f.label :uploads %><br>
  <%= f.file_field :uploads, multiple: true %>
</p>

<p>
   <%= f.submit "Save" %>
 </p>

 <% end %>
