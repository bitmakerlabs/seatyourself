<% if menu_item.errors.any? %>
   <div id="error_explanation">
     <p><%= pluralize(menu_item.errors.count, "error") %> prohibited this menu item from being saved:</p>
     <ul>
     <% menu_item.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
     </ul>
   </div>
<% end %>
<%= form_for [restaurant, menu_item] do |f| %>
  <p>
    <%= f.label :dish %><br/>
    <%= f.text_field :dish %>
  </p>
  <p>
    <%= f.label :price %><br/>
    <%= f.number_field :price %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
